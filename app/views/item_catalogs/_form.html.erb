<%= form_for(@item_catalog) do |f| %>
  <% if @item_catalog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_catalog.errors.count, "error") %> prohibited this item_catalog from being saved:</h2>

      <ul>
      <% @item_catalog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
